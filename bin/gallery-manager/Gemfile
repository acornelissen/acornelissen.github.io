# Dependencies for the local gallery manager only. Kept separate from the
# site's Gemfile so running this tool can never disturb the Jekyll lockfile.
# Everything here is development-only and is never published.

source "https://rubygems.org"

gem "puma", "~> 6.6"
gem "rackup", "~> 2.2"
gem "sinatra", "~> 4.1"

group :test do
  gem "minitest", "~> 5.25"
  gem "rack-test", "~> 2.2"
end
